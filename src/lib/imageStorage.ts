const cloudName=process.env.NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME;
const uploadPreset=process.env.NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET;
export const cloudinaryConfigured=Boolean(cloudName&&uploadPreset);
interface CloudinaryResponse{secure_url?:string;error?:{message?:string}}
export async function uploadPlaceImage(file:File,folder:string){if(!cloudName||!uploadPreset)throw new Error("Cloudinary belum dikonfigurasi. Cloud name dan unsigned upload preset wajib diisi.");const body=new FormData();body.append("file",file);body.append("upload_preset",uploadPreset);body.append("folder",folder);const controller=new AbortController();const timer=window.setTimeout(()=>controller.abort(),20000);try{const response=await fetch(`https://api.cloudinary.com/v1_1/${encodeURIComponent(cloudName)}/image/upload`,{method:"POST",body,signal:controller.signal});const result=await response.json() as CloudinaryResponse;if(!response.ok||!result.secure_url)throw new Error(result.error?.message||"Upload foto ke Cloudinary gagal.");return result.secure_url}catch(error){if(error instanceof DOMException&&error.name==="AbortError")throw new Error("Upload foto ke Cloudinary terlalu lama. Periksa koneksi lalu coba lagi.");throw error}finally{window.clearTimeout(timer)}}