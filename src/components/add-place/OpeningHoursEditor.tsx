import type { OpeningHours } from "@/types/place";
const days=["monday","tuesday","wednesday","thursday","friday","saturday","sunday"] as const;
const names=["Senin","Selasa","Rabu","Kamis","Jumat","Sabtu","Minggu"];
export function OpeningHoursEditor({value,onChange}:{value:OpeningHours;onChange:(v:OpeningHours)=>void}){const update=(day:typeof days[number],patch:Partial<OpeningHours[typeof day]>)=>onChange({...value,[day]:{...value[day],...patch}});const copy=()=>{const first=value.monday;onChange(Object.fromEntries(days.map(d=>[d,{...first}])) as unknown as OpeningHours)};return <div><button className="add-secondary-button" type="button" onClick={copy}>Samakan semua dengan Senin</button><div className="hours-list">{days.map((day,i)=><div className="hours-row" key={day}><strong>{names[i]}</strong><label className="closed-check"><input type="checkbox" checked={value[day].closed} onChange={e=>update(day,{closed:e.target.checked})}/> Tutup</label><input aria-label={`Jam buka ${names[i]}`} type="time" disabled={value[day].closed} value={value[day].open} onChange={e=>update(day,{open:e.target.value})}/><span>–</span><input aria-label={`Jam tutup ${names[i]}`} type="time" disabled={value[day].closed} value={value[day].close} onChange={e=>update(day,{close:e.target.value})}/></div>)}</div></div>}